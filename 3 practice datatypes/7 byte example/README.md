# Ошибка работы с byte

## Объяснение
- Выражение `a + 10` имеет тип `int`. Попытка присвоить результат переменной `byte` без приведения типов вызывает ошибку.
- После приведения типов (`(byte)`), возможна потеря данных.